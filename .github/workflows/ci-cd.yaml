name: Build and deploy MeatShopApp

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-deploy:
    name: Build and deploy MeatShopApp
    runs-on: self-hosted
    steps:
      - name: docker compose build
        run: docker compose up ~/MeatShop/Docker
